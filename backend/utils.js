//create a function to genrate a random Id Eg : SHIRT5822ID , ALEN2002KLF  any random id with integer and letters
const generateId = (body) => {

    //write code here and return as result

    return result
}

module.exports = { generateId }